#include<stdio.h>
void main()
{
	int number;
	printf("enter the number");
	scanf("%d", &number);
	if(number>0)
	{
		printf("the given number is poisitve");
	}
	else
	{
		printf("the given number is negative");
	}
}
